# Creates graph
CreateGraph graph0

# Adds a node to a graph
AddNode graph0 node0

# Adds another node to a graph
AddNode graph0 node1

# Add edge from parent node to child node in graph
AddEdge graph0 node0 node1 zeroToOne

# Lists children of parent node in graph
ListChildren graph0 node0